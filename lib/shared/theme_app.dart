

import 'package:flutter/material.dart';

class ThemeApp{
 static const Color blackScreen=Color(0XFF1E1E1E);
 static const Color selectCategory=Color(0XFFFFB224);
 static const Color  unselectedCategory=Color(0XFFC6C6C6);
 static const Color  backgroundColorBottomNavigationBarItem=Color(0XFF707070);
 static const Color  backgroundList=Color(0XFF282A28);

 static const Color backgroundItem=Color(0XFF343534);
 static ThemeData lightTheme =ThemeData(
   scaffoldBackgroundColor: blackScreen ,

 );
}